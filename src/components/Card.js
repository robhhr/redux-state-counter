import React from "react";
import Title from "./Title/Title";

const Card = () => {
  return (
    <div className="card">
      <Title title={"React Toggler"} />
    </div>
  );
};

export default Card;
